services:
  - type: worker
    name: arb-bot
    runtime: python
    runtimeVersion: python-3.11.8
    envVars:
      - key: TELEGRAM_TOKEN
        generateValue: true
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    plan: free
